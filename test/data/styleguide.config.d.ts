// Types for the fixture config, so that specs can import it without casts
import type { StyleguidistConfig } from '../../src/typings/index.js';

declare const config: StyleguidistConfig;
export default config;
