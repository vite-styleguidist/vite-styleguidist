Custom Endpoint Example

The example component invokes the `/custom` endpoint added to the running style guide dev server by the `configureServer` option in `styleguide.config.js` (it only exists while `styleguidist server` is running, not in a static build).

```js
<CustomEndpoint />
```
