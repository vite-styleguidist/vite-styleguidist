// A project with both a JavaScript and a TypeScript config: the JavaScript one is the one
// Styleguidist has always loaded, and it stays the one that wins
export default {
	title: 'JS Style Guide',
};
