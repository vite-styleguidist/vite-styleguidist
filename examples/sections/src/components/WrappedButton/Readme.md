react-docgen is not able to parse prop types of enhanced components that wrap a component imported from another file, so this component documents no props. Styleguidist will add it to your style guide anyway.

```jsx
<WrappedButton>I'm a wrapped button</WrappedButton>
```
